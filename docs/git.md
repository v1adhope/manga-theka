# Git context

## Conventions

- Branch naming: `feat/`, `fix/`, `chore/` prefixes.
- Squash merge to main.
- Commit messages: use the `/caveman-commit` skill.
- Push over HTTPS; leave `origin` and the branch upstream as they are.
